# Open work — handoff for the next session

Status: **Written 2026-08-19** at the founder's request, at the end of a long session. Everything
below is committed and pushed on `feat/buddy-3d-and-reminders`. Read `Current_Context.md` and
`06_Decisions/Decision_Log.md` (through D63) first; this file is only the open list.

**How to use this:** Section 1 needs nobody but you — start there and work down. Section 2 cannot
move without the founder, so raise those in conversation while Section 1 is building.

---

## 1. Buildable now, no founder input needed

### 1.1 The streak is roughly right; the app never SHOWS which Steps bind
**Refined by the founder, 2026-08-19, and this supersedes his first framing an hour earlier.**

He first said a streak day should mean "I did all of today's Steps". On reflection he corrected
himself, and the corrected version is the one to build:

> *"אם משימה לא מחויבת ליום מסוים היא יכולה גם להופיע למחרת וזה בסדר — זה לא צריך לשבור את ה־streak.
> הבעיה לדעתי שויזואלית זה נראה באפליקציה … צריך שיופיע חיווי של recommended, ואז אי ביצוע שלו לא
> שובר את ה־streak."*

So the shipped LOGIC is close to correct. A workout that could happen Wednesday *or* Thursday
carries slack, and missing it on Wednesday is not a failure — exactly what
`app/src/core/config/streak.ts` (D26.4) already encodes: only a Step with **no slack left in the
week** breaks the streak.

**The actual defect is that the user cannot see the difference.** Two Steps render identically on
Home. He missed one, his streak rose, and nothing on screen explained why — which is why it read as
a bug. A rule the user cannot see is a rule the user cannot trust.

**What to build — a VISUAL distinction, not a new rule:**
- A Step **with slack** is marked **`recommended`** (his word). Missing it never touches the streak,
  and the label is what says so before the fact rather than after.
- A Step with **no slack left** is the binding one: doing it today is what holds the week together,
  and missing it is what breaks the streak.
- The engine already computes this (the urgency test behind `STREAK_CONFIG`), so this is surfacing
  an existing derivation, not inventing one. Do NOT fork a second definition in the UI — read the
  same one, the way `core/util/milestones.ts` is shared.

**Do not change D26.4's rule.** The founder has now confirmed it twice over; what was missing was
ever telling him about it.

**Copy warning:** `recommended` must never read as optional-and-therefore-unimportant, and the
binding state must never read as a threat. This is the app that promises no penalty for a life that
got in the way — the difference being shown is about the WEEK's arithmetic, not about the user.

### 1.2 Merge Postpone and Reschedule into one action
Founder on device: the two read as the same thing. Keep **one** action, "Postpone"; after tapping,
the user chooses a specific time or an automatic one, and may add a reason. Today they are two rows
in the report sheet with overlapping meaning (`components/journey/RecoveryFlow.tsx`).

### 1.3 The shared completion image loses the whole design
**The most visible of these — it is what the partner will see.** The completion card is designed
and looks right on screen; tapping Share sends plain text ("You did it / Build core strength") with
none of it. It must share as an IMAGE identical to what was shown. Needs a view-to-image capture.
Check first whether that is possible without a new native dependency; if it is not, say so and let
the founder decide before adding one.

### 1.4 Options for a completed card's background
He likes the current completed-Step design and wants to consider a more prominent background so a
done card stands out. **Render the options for him to look at** — he has said repeatedly that he
wants to see, not read descriptions.

### 1.5 Home's scroll-to-top is still unverified
Fixed in a previous session, never seen working on a device. Not a code task — ask him to
double-tap the Home tab and say whether it scrolls up.

---

## 2. Blocked on the founder

### 2.1 The proxy's last mile (he is doing this)
The `gemini-proxy` function is deployed and verified answering 401 without auth. Still open: run
`supabase/migrations/0002_llm_usage.sql` in the SQL editor — **without that table the 2 MB per-user
cap is not enforced at all**. Then the confirmation: use the Coach, then
`select user_id, bytes, requests, last_at from public.llm_usage order by last_at desc limit 5;`
A row means it went through the server.

### 2.2 Apple / Google sign-in
His console setup is DONE (Apple capability + Services ID + key; Google consent screen, test users,
Web + iOS client IDs, Supabase providers). **The two Google client IDs are DONE** — they are in
`app/.env` as `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` and `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID`, documented
in `.env.example`, and they must also be added to the EAS environment before the build that goes to
the partner (`.env` is not committed, so a cloud build will not see them otherwise). Nothing else
is needed from him to start the code. Then the code: two native dependencies, the two
gateway methods that currently throw `AuthNotAvailableError`, a sign-in screen, session wiring.

**Sequencing that costs a day if missed:** native dependencies mean a NEW BUILD. Every native change
must land before the single build sent to the partner.

### 2.3 Getting the app onto the partner's device
Separate work from sign-in, and also needs him: register the partner's device with Apple, build with
internal distribution, send the install link. TestFlight is the slower path (review can take days);
device registration is hours.

### 2.4 Release gates (block the store, not the partner)
No privacy policy · authenticated encryption designed but not implemented · no recovery tool for
quarantined data · `delete-account` written but never deployed · Support Circle authorization never
tested against the live database with two real accounts.

---

## 3. Two standing instructions worth repeating to whoever picks this up

**Ask complete questions.** The founder has objected more than once to short questions that assume
vocabulary he was never given. Do not use a term ("axis", "signal type", "bottleneck", "arc")
without defining it in the same message.

**He wants to see, not read.** For anything with a visual answer, render options.
