Implement the PUSh Hopper character using this package.

1. Load `model/hopper_v1.glb` and preserve all mesh/node names.
2. Treat `assets/push_face_neutral.svg` as canonical; never redraw or substitute its silhouette.
3. Use `anchors.json` for wearables and held items.
4. Use `materials.json` as the initial PBR definition.
5. Implement mastery states from `mastery.json`.
6. Support hat, shirt, pants, shoes, cape, necklace, wrist items and two held items.
7. Keep body-part meshes separate for later rigging and cosmetic replacement.
8. Keep the API generic for future species.
