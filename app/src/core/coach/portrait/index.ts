/**
 * THE PORTRAIT (דיוקן) — the barrel.
 *
 * What we understand about the person, built by talking rather than by a form: the types and their
 * closed taxonomies ({@link ./types}), the merge rules that protect what somebody actually said
 * ({@link ./merge}), the gaps that drive what is asked next ({@link ./gaps}), the reader that turns
 * a transcript into validated field updates ({@link ./readPortrait}), and the one function that
 * composes the understanding check out of it ({@link ./facts}).
 */
export * from './types';
export * from './merge';
export * from './gaps';
export * from './readPortrait';
export * from './facts';
