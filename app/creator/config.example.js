/*
 * Copy to `config.js`, or run `npm run creator:config` to write it from the
 * app's `.env`. Gitignored for the same reason `.env` is: one source of truth
 * for which Supabase project is live.
 */
window.PUSHAPP_STUDIO = {
  supabaseUrl: 'https://YOUR-PROJECT.supabase.co',
  supabaseAnonKey: 'sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx',
};
