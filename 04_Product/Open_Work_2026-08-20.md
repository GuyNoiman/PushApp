# Open work — handoff, end of 2026-08-20

Status: **Written 2026-08-20 at the founder's request, at the end of a long session.** Everything
below is committed and pushed on `feat/buddy-3d-and-reminders`. Supersedes
`Open_Work_2026-08-19.md` as the live list; that file stays as the record of the previous day and
of what came off it.

**How to use this:** §1 needs nobody but you. §2 cannot move without the founder. §3 is the standing
context that makes the rest safe to work on — read it before touching anything native.

---

## 0. Where things stand

- Branch `feat/buddy-3d-and-reminders`, **jest 1744 / 174 suites**, `tsc` clean, working tree clean.
- **Build 3 (version 1.0.0) is in TestFlight**, App Store Connect app id `6803091892`. The partner
  (`liamsh1979@gmail.com`) is on the team as Marketing and was added as a tester; whether the
  invitation actually reached him was still being checked on the evening of 2026-08-20.
- **Everything built after that build exists only in git.** It has NOT reached the partner's phone.
  See §2.1 — publishing it is one command and it is the single highest-value thing outstanding.

---

## 1. Buildable now, no founder input needed

### 1.1 The Career library is unreachable — it needs a DIAGNOSIS
Eighteen Journeys in six goal families are ingested, validated, translated and tested
(`app/src/core/learning/library/career/`). Nothing routes a real conversation to them, because
choosing a family requires the Career expert to work out WHICH of the six a person's goal is —
unclear target, missing proof, no access, and the three FIND_DIRECTION families — and the experts do
not diagnose. Each returns one hardcoded arc.

The letter drafted for the partner (`Partner_Letter_2026-08-19_Library_Ingest.md` §5.2) asks him for
exactly this, because it is domain work rather than engineering. **Until it lands, the content is
correct and inert**, and `AppCore.matchVariant` deliberately refuses to stamp provenance from a
Journey whose content was not the one built.

### 1.2 The remaining pieces of the redesign
From the founder's mockups, still not built:
- **The profile photo beside the greeting.** Home shows a monogram today; profile photos are Phase 2
  in the profile layer, and a grey placeholder head would promise a feature that does not exist.
- **The top status strip's own treatment** (the level/XP/streak cluster is unchanged since August).
- **The immersive, full-bleed variant of the people section** (his first image).
- **"Needs you" on Home** — he said it is an upgrade of the existing people carousel rather than a
  new module, and did not say what to change. **Ask before building.**

### 1.3 The six "Coming" tiles in Tools
Weekly reflection, Breathe, Strengths map, Focus timer, Act of kindness, For a hard day. They are
named on the screen and deliberately not pressable. Each is a real piece of work; none is specified.

### 1.4 iPad is portrait-only
`supportsTablet` is declared and the partner tests on an iPad, but the app does not rotate. Deciding
to support landscape is a design decision with real layout consequences (the week card, the
carousels), not a config flag.

---

## 2. Blocked on the founder

### 2.1 PUBLISH THE UPDATE — one command, and it is the most valuable thing on this list
Everything from 2026-08-19 evening onward — the whole redesign, the RTL fix that makes Hebrew
onboarding work, the name-leak fix, Tools, the nav change — is in git and **not** on the partner's
phone. Build 3 subscribes to the `production` channel, and the fingerprint has not moved, so it is:

```
npx eas-cli@latest update --branch production --message "Home redesign, Hebrew onboarding fix"
```

Run from `app/`. It also proves the over-the-air pipeline works, which nothing has yet.

### 2.2 The partner's TestFlight invitation
He was added as an individual tester on build 3 and showed `No Builds Available`, which almost
always means his App Store Connect user invitation was never accepted. Check
`https://appstoreconnect.apple.com/access/users`: a `Pending` row has a **Resend Invitation** button,
and he must accept it with an Apple ID whose address is exactly `liamsh1979@gmail.com`. Adding him to
the **Team (Expo)** internal group (which already holds build 3) is the more reliable route than the
per-build individual tester.

### 2.3 Send the partner letter
`04_Product/Partner_Letter_2026-08-19_Library_Ingest.md`. A forwardable copy without the founder
notes was sent to him in chat on 2026-08-19. Its real ask is §5.2, the diagnosis (see §1.1).

### 2.4 One device check nobody else can do
Double-tap the Home tab: does the page scroll back to the top? Open since several sessions
(`Open_Work_2026-08-19.md` §1.5).

### 2.5 Release gates (block the store, not the partner)
No privacy policy · authenticated encryption designed but not implemented · no recovery tool for
quarantined data · Support Circle authorization never tested against the live database with two real
accounts · EU trader status not provided (blocks EU distribution only).

---

## 3. Standing context — read before touching anything native

### 3.1 The fingerprint rule
The partner's build carries iOS runtime fingerprint **`df6c2127bbd3be3766774e3f008f4ae5158306cd`**.
Updates only reach a binary whose fingerprint MATCHES. **A native dependency, any `app.json` edit, or
an SDK bump changes it and cuts his phone off from every update we publish.** Not native, and
therefore free: all TypeScript, every component, i18n copy, colours, artwork drawn in code, and fonts
(loaded at runtime from JS assets — verified: adding two font families left the hash untouched).
`expo-blur` and `expo-linear-gradient` were added BEFORE the build precisely so the redesign would
never need one. If a native change becomes genuinely necessary, it means a new build and the partner
has to install it: raise it, never do it as a side effect.

### 3.2 The tab bar exists twice
`app-tabs.tsx` (native) and `app-tabs.web.tsx` (web) are separate files kept in sync BY HAND. A tab
added, removed or renamed in one is invisible in the other. This cost an hour on 2026-08-20.

### 3.3 Nobody's name in shipped copy
`src/i18n/__tests__/noPersonalData.test.ts` sweeps every resource file for development identities and
fails if one ships. It exists because the coach's greeting said "Hi Guy" to every user, in both
languages, for months. Add a name to that blocklist when a new person works on the app; never add a
user's data to it.

### 3.4 A release build cannot invent data
The demo seed and the simulated identity are gated on `__DEV__` as well as on their env vars
(`core/config/featureFlags.ts`, `core/profile/simulatedUser.ts`), so a shipped app cannot seed
Journeys or show a developer's name however the environment is configured.
`src/core/__tests__/cleanFirstRun.test.ts` holds that down.

### 3.5 Two standing instructions about the founder
**Ask complete questions** — never use a term without defining it in the same message. **He wants to
see, not read** — for anything with a visual answer, render options rather than describing them.
